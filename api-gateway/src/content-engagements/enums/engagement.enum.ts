export enum InteractionNature {
  explicit = 'explicit',
  implicit = 'implicit',
  friction = 'friction',
}

export enum ActionType {
  answered = 'answered',
  liked = 'liked',
  downvoted = 'downvoted',
  bookmarked = 'bookmarked',
  ignored = 'ignored',
  abandoned = 'abandoned',
}
